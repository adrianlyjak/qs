name: research

control-plane:
  port: 8000

default-service: echo_workflow

services:
  echo_workflow:
    name: Echo Workflow
    source:
      type: local
      name: src
    path: src/quick_start.workflow:echo_workflow

ui:
  name: My Nextjs App
  source:
    type: local
    name: ui
